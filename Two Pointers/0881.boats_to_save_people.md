# 881.救生艇

题目链接：[881.救生艇](https://leetcode.cn/problems/boats-to-save-people/)

## 题目大意

给定数组 people。people[i]表示第 i 个人的体重 ，船的数量不限，每艘船可以承载的最大重量为 limit

每艘船最多可同时载两人，但条件是这些人的重量之和最多为 limit

返回 承载所有人所需的最小船数 

```js
Example 1:
Input: people = [1,2], limit = 3
Output: 1
Explanation: 1 boat (1, 2)

Example 2:
Input: people = [3,2,2,1], limit = 3
Output: 3
Explanation: 3 boats (1, 2), (2) and (3)

Example 3:
Input: people = [3,5,3,4], limit = 5
Output: 4
Explanation: 4 boats (3), (3), (4), (5)
```

限制:
- 1 <= people.length <= 5 * 10^4
- 1 <= people[i] <= limit <= 3 * 10^4

## 解题

一个直观的想法是：由于一个船要么载两人要么载一人，在人数给定的情况下，为了让使用的总船数最小，要当尽可能让更多船载两人，即尽可能多的构造出数量之和不超过 limit 的二元组。

先对 people 进行排序，然后使用两个指针 l 和 r 分别从首尾开始进行匹配：
- 如果 people[l]+people[r]<=limit，说明两者可以同船，此时船的数量加一，两个指针分别往中间靠拢
- 如果 people[l]+people[r]>limit，说明不能成组，由于题目确保人的重量不会超过 limit，此时让 people[r] 独立成船，船的数量加一，r 指针左移

```js
/**
 * @param {number[]} people
 * @param {number} limit
 * @return {number}
 */
var numRescueBoats = function(people, limit) {
    people.sort((a, b) => a - b);

    let left = 0;
    let right = people.length - 1;
    let boats = 0;

    while (left <= right) {
        // Always put the heaviest remaining person on a boat.
        // If the lightest remaining person can fit with them, pair them together.
        if (people[left] + people[right] <= limit) {
            left++;
        }

        // The heaviest person is always handled in this round.
        right--;
        boats++;
    }

    return boats;
};
```
```python
from typing import List

class Solution:
    def numRescueBoats(self, people: List[int], limit: int) -> int:
        people.sort()

        left = 0
        right = len(people) - 1
        boats = 0

        while left <= right:
            # Always put the heaviest remaining person on a boat.
            # If the lightest person can go with them, pair them together.
            if people[left] + people[right] <= limit:
                left += 1

            # The heaviest person always uses one boat.
            right -= 1
            boats += 1

        return boats
```
```java
import java.util.Arrays;

class Solution {
    public int numRescueBoats(int[] people, int limit) {
        Arrays.sort(people);

        int left = 0;
        int right = people.length - 1;
        int boats = 0;

        while (left <= right) {
            // Always put the heaviest remaining person on a boat.
            // If the lightest remaining person can fit with them, pair them together.
            if (people[left] + people[right] <= limit) {
                left++;
            }

            // The heaviest person is always handled in this round.
            right--;
            boats++;
        }

        return boats;
    }
}
```

- 时间复杂度：排序复杂度为 `O(nlogn)`；双指针统计答案复杂度为 `O(n)`。整体复杂度为 `O(nlogn)`
- 空间复杂度：`O(logn)`