# 2333.最小差值平方和

题目链接：[2333.最小差值平方和](https://leetcode.cn/problems/minimum-sum-of-squared-difference/)

## 题目大意

给你两个下标从 0 开始的整数数组 nums1 和 nums2 ，长度为 n 

数组 nums1 和 nums2 的 差值平方和 定义为所有满足 0 <= i < n 的 (nums1[i] - nums2[i])2 之和

同时给你两个正整数 k1 和 k2 。你可以将 nums1 中的任意元素 +1 或者 -1 至多 k1 次。类似的，你可以将 nums2 中的任意元素 +1 或者 -1 至多 k2 次

请你返回修改数组 nums1 至多 k1 次且修改数组 nums2 至多 k2 次后的最小 差值平方和 

注意：你可以将数组中的元素变成 负 整数

```js
Example 1:
Input: nums1 = [1,2,3,4], nums2 = [2,10,20,19], k1 = 0, k2 = 0
Output: 579
Explanation: The elements in nums1 and nums2 cannot be modified because k1 = 0 and k2 = 0. 
The sum of square difference will be: (1 - 2)2 + (2 - 10)2 + (3 - 20)2 + (4 - 19)2 = 579.

Example 2:
Input: nums1 = [1,4,10,12], nums2 = [5,8,6,9], k1 = 1, k2 = 1
Output: 43
Explanation: One way to obtain the minimum sum of square difference is: 
- Increase nums1[0] once.
- Increase nums2[2] once.
The minimum of the sum of square difference will be: 
(2 - 5)2 + (4 - 8)2 + (10 - 7)2 + (12 - 9)2 = 43.
Note that, there are other ways to obtain the minimum of the sum of square difference, but there is no way to obtain a sum smaller than 43.
```

限制：
- n == nums1.length == nums2.length
- 1 <= n <= 10^5
- 0 <= nums1[i], nums2[i] <= 10^5
- 0 <= k1, k2 <= 10^9

## 解题

要求最终差值平方和最小，那肯定是尽可能减小最大的差值（因为值越大，平方之后增加得越多）

先遍历一次数组，计算出所有差值的绝对值（用 arr 数组存储，arr[i] 表示差值为 i + 1 出现的次数），然后从大到小尝试减小最大值的数量。将 k (k = k1 + k2，因为目标是消灭最大差值，和调整 nums1 还是 nums2 无关) 消耗完之后，计算一下平方和就是结果了

```js
/**
 * @param {number[]} nums1
 * @param {number[]} nums2
 * @param {number} k1
 * @param {number} k2
 * @return {number}
 */
var minSumSquareDiff = function(nums1, nums2, k1, k2) {
    const count = new Array(100001).fill(0);

    for (let i = 0; i < nums1.length; i++) {
        const diff = Math.abs(nums1[i] - nums2[i]);
        count[diff]++;
    }

    let k = k1 + k2;

    for (let diff = count.length - 1; diff > 0; diff--) {
        if (k === 0) {
            break;
        }

        const change = Math.min(k, count[diff]);

        count[diff - 1] += change;
        count[diff] -= change;
        k -= change;
    }

    let res = 0;

    for (let diff = 0; diff < count.length; diff++) {
        if (count[diff] !== 0) {
            res += count[diff] * diff * diff;
        }
    }

    return res;
};
```
```python
class Solution:
    def minSumSquareDiff(self, nums1: List[int], nums2: List[int], k1: int, k2: int) -> int:
        count = [0] * (100001)
        for i in range(len(nums1)):
            count[abs(nums1[i] - nums2[i])] += 1
        k = k1 + k2
        for i in range(len(count) - 1, 0, -1):
            change = min(k, count[i])
            count[i - 1] += change
            k -= change
            count[i] -= change
        res = 0
        for i in range(len(count)):
            if count[i] != 0:
                res += count[i] * i * i 
        return res
```
```java
import java.util.*;

class Solution {
    public long minSumSquareDiff(int[] nums1, int[] nums2, int k1, int k2) {
        long[] count = new long[100001];

        for (int i = 0; i < nums1.length; i++) {
            int diff = Math.abs(nums1[i] - nums2[i]);
            count[diff]++;
        }

        long k = (long) k1 + k2;

        for (int diff = count.length - 1; diff > 0; diff--) {
            if (k == 0) {
                break;
            }

            long change = Math.min(k, count[diff]);

            count[diff - 1] += change;
            count[diff] -= change;
            k -= change;
        }

        long res = 0;

        for (int diff = 0; diff < count.length; diff++) {
            if (count[diff] != 0) {
                res += count[diff] * diff * diff;
            }
        }

        return res;
    }
}
```


- 时间复杂度：`O(n + C)`
  - 构建差值频率桶需要遍历所有元素一次 -> O(n)
  - 从大到小消耗操作次数，最多扫描整个差值范围 C -> O(C)
  - 计算最终平方和再次扫描整个桶，O(C)
  - `O(n) + O(C) + O(C) = O(n + C)`
  - 因为本题中 C = 100000 是固定上限，所以也可以说：O(n + 100000)
  - 在面试中可以进一步说：如果把 100000 看作常数，则近似为 O(n)
- 空间复杂度是 `O(C)`，因为我们使用了一个大小为 `C + 1` 的计数桶。在本题约束下 `C = 100000`，所以可以认为额外空间是固定大小