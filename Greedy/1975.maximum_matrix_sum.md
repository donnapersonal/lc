# 1975.最大方阵和

题目链接：[1975.最大方阵和](https://leetcode.cn/problems/maximum-matrix-sum/)

## 题目大意

给你一个 n x n 的整数方阵 matrix 。你可以执行以下操作 任意次 ：
- 选择 matrix 中 相邻 两个元素，并将它们都 乘以 -1 
  
如果两个元素有 公共边 ，那么它们就是 相邻 的

你的目的是 最大化 方阵元素的和。请你在执行以上操作之后，返回方阵的 最大 和

![alt text](https://github.com/donnapersonal/picx-images-hosting/raw/master/image.8l0pgmx7xh.webp)

限制：
- n == matrix.length == matrix[i].length
- 2 <= n <= 250
- -10^5 <= matrix[i][j] <= 10^5

## 解题

```js
/**
 * @param {number[][]} matrix
 * @return {number}
 */
var maxMatrixSum = function(matrix) {
    let total = 0;
    let negCount = 0;
    let minAbs = Infinity;

    for (const row of matrix) {
        for (const num of row) {
            const absVal = Math.abs(num);

            total += absVal;
            minAbs = Math.min(minAbs, absVal);

            if (num < 0) {
                negCount++;
            }
        }
    }

    if (negCount % 2 === 0) {
        return total;
    }

    return total - 2 * minAbs;
};
```
```python
from typing import List
from math import inf

class Solution:
    def maxMatrixSum(self, matrix: List[List[int]]) -> int:
        total = 0
        neg_count = 0
        min_abs = inf

        for row in matrix:
            for num in row:
                abs_val = abs(num)

                total += abs_val
                min_abs = min(min_abs, abs_val)

                if num < 0:
                    neg_count += 1

        if neg_count % 2 == 0:
            return total

        return total - 2 * min_abs

# 写法 2
class Solution:
    def maxMatrixSum(self, matrix: List[List[int]]) -> int:
        total = neg_cnt = 0
        mn = inf
        for row in matrix:
            for x in row:
                if x < 0:
                    neg_cnt += 1
                    x = -x  # 先把负数都变成正数
                mn = min(mn, x)
                total += x

        if neg_cnt % 2:
            total -= mn * 2  # 给绝对值最小的数添加负号
        return total
```
```java
class Solution {
    public long maxMatrixSum(int[][] matrix) {
        long total = 0;
        int negCount = 0;
        int minAbs = Integer.MAX_VALUE;

        for (int[] row : matrix) {
            for (int num : row) {
                int absVal = Math.abs(num);

                total += absVal;
                minAbs = Math.min(minAbs, absVal);

                if (num < 0) {
                    negCount++;
                }
            }
        }

        if (negCount % 2 == 0) {
            return total;
        }

        return total - 2L * minAbs;
    }
}
```

- 时间复杂度：`O(mn)`，其中 `m` 和 `n` 分别是 `matrix` 的行数和列数
- 空间复杂度：`O(1)`