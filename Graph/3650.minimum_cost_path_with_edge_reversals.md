# 3650.边反转的最小路径总成本

题目链接：[3650.边反转的最小路径总成本](https://leetcode.cn/problems/minimum-cost-path-with-edge-reversals/)

## 题目大意

给你一个包含 n 个节点的有向带权图，节点编号从 0 到 n - 1。同时给你一个数组 edges，其中 edges[i] = [ui, vi, wi] 表示一条从节点 ui 到节点 vi 的有向边，其成本为 wi

每个节点 ui 都有一个 最多可使用一次 的开关：当你到达 ui 且尚未使用其开关时，你可以对其一条入边 vi → ui 激活开关，将该边反转为 ui → vi 并 立即 穿过它

反转仅对那一次移动有效，使用反转边的成本为 2 * wi

返回从节点 0 到达节点 n - 1 的 最小 总成本。如果无法到达，则返回 -1

![example](https://github.com/donnapersonal/picx-images-hosting/raw/master/image.3gp0sa61kr.webp)

限制：
- 2 <= n <= 5 * 10^4
- 1 <= edges.length <= 10^5
- edges[i] = [ui, vi, wi]
- 0 <= ui, vi <= n - 1
- 1 <= wi <= 1000

## 解题

根据 Dijkstra 算法，同一个节点我们只会访问一次，所以「最多可使用一次开关」这个约束是多余的，我们只需把反向边的边权设置为 2wi 即可，答案为 0 到 n−1 的最短路长度

```js
/**
 * @param {number} n
 * @param {number[][]} edges
 * @return {number}
 */
var minCost = function(n, edges) {
    const graph = Array.from({ length: n }, () => []);

    for (const [x, y, wt] of edges) {
        graph[x].push([y, wt]);
        graph[y].push([x, wt * 2]);
    }

    const dist = Array(n).fill(Infinity);
    dist[0] = 0;

    const heap = new MinHeap();
    heap.push([0, 0]); // [distance, node]

    while (!heap.isEmpty()) {
        const [curDist, node] = heap.pop();

        if (curDist > dist[node]) {
            continue;
        }

        if (node === n - 1) {
            return curDist;
        }

        for (const [nei, weight] of graph[node]) {
            const newDist = curDist + weight;

            if (newDist < dist[nei]) {
                dist[nei] = newDist;
                heap.push([newDist, nei]);
            }
        }
    }

    return -1;
};

class MinHeap {
    constructor() {
        this.heap = [];
    }

    isEmpty() {
        return this.heap.length === 0;
    }

    push(item) {
        this.heap.push(item);
        this.bubbleUp(this.heap.length - 1);
    }

    pop() {
        if (this.heap.length === 1) {
            return this.heap.pop();
        }

        const min = this.heap[0];
        this.heap[0] = this.heap.pop();
        this.bubbleDown(0);

        return min;
    }

    bubbleUp(index) {
        while (index > 0) {
            const parent = Math.floor((index - 1) / 2);

            if (this.heap[parent][0] <= this.heap[index][0]) {
                break;
            }

            [this.heap[parent], this.heap[index]] = [this.heap[index], this.heap[parent]];
            index = parent;
        }
    }

    bubbleDown(index) {
        const n = this.heap.length;

        while (true) {
            let smallest = index;
            const left = index * 2 + 1;
            const right = index * 2 + 2;

            if (left < n && this.heap[left][0] < this.heap[smallest][0]) {
                smallest = left;
            }

            if (right < n && this.heap[right][0] < this.heap[smallest][0]) {
                smallest = right;
            }

            if (smallest === index) {
                break;
            }

            [this.heap[smallest], this.heap[index]] = [this.heap[index], this.heap[smallest]];
            index = smallest;
        }
    }
}
```
```python
class Solution:
    def minCost(self, n: int, edges: List[List[int]]) -> int:
        graph = [[] for _ in range(n)]  # 邻接表
        for x, y, wt in edges:
            graph[x].append((y, wt))
            graph[y].append((x, wt * 2))

        dis = [inf] * n
        dis[0] = 0  # 起点到自己的距离是 0
        h = [(0, 0)]  # 堆中保存 (起点到节点 x 的最短路长度，节点 x)

        while h:
            dis_x, x = heappop(h)
            if dis_x > dis[x]:  # x 之前出堆过
                continue
            if x == n - 1:  # 到达终点
                return dis_x
            for y, wt in graph[x]:
                new_dis_y = dis_x + wt
                if new_dis_y < dis[y]:
                    dis[y] = new_dis_y  # 更新 x 的邻居的最短路
                    # 懒更新堆：只插入数据，不更新堆中数据
                    # 相同节点可能有多个不同的 new_dis_y，除了最小的 new_dis_y，其余值都会触发上面的 continue
                    heappush(h, (new_dis_y, y))

        return -1
```
```java
import java.util.*;

class Solution {
    public int minCost(int n, int[][] edges) {
        List<int[]>[] graph = new ArrayList[n];

        for (int i = 0; i < n; i++) {
            graph[i] = new ArrayList<>();
        }

        for (int[] edge : edges) {
            int x = edge[0];
            int y = edge[1];
            int wt = edge[2];

            graph[x].add(new int[]{y, wt});
            graph[y].add(new int[]{x, wt * 2});
        }

        int[] dist = new int[n];
        Arrays.fill(dist, Integer.MAX_VALUE);
        dist[0] = 0;

        PriorityQueue<int[]> pq = new PriorityQueue<>((a, b) -> a[0] - b[0]);
        pq.offer(new int[]{0, 0}); // {distance, node}

        while (!pq.isEmpty()) {
            int[] cur = pq.poll();
            int curDist = cur[0];
            int node = cur[1];

            if (curDist > dist[node]) {
                continue;
            }

            if (node == n - 1) {
                return curDist;
            }

            for (int[] next : graph[node]) {
                int nei = next[0];
                int weight = next[1];

                int newDist = curDist + weight;

                if (newDist < dist[nei]) {
                    dist[nei] = newDist;
                    pq.offer(new int[]{newDist, nei});
                }
            }
        }

        return -1;
    }
}
```

- 时间复杂度：`O(n+mlogm)`，其中 `m` 是 `edges` 的长度
- 空间复杂度：`O(n+m)`